{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended", "helpers:disableTypesNodeMajor"],
  "schedule": ["before 9am on monday"],
  "prConcurrentLimit": 10,
  "automerge": true,
  "platformAutomerge": true,
  "packageRules": [
    {
      "description": "Automerge is gated on branch protection: the main ruleset requires a pull request and green Verify Templates checks, and GitHub's native auto-merge completes the merge once they pass. Renovate no longer merges directly - platformAutomerge hands that to GitHub, so a Renovate PR is subject to exactly the same gate as a human one.",
      "matchPackageNames": ["*"]
    },
    {
      "description": "typescript-eslint peers typescript <6.1.0. Without this cap Renovate opens a TypeScript 7 PR every week that verify.sh correctly rejects.",
      "matchPackageNames": ["typescript"],
      "allowedVersions": "<6.1.0"
    },
    {
      "description": "Node baseline is a deliberate LTS policy decision. Renovate would push .nvmrc to the current non-LTS release.",
      "matchManagers": ["nvm"],
      "enabled": false
    },
    {
      "description": "Digest pinning is right for a generated project but wrong inside template files, where it would emit scaffolds full of SHAs that read as stale on day one.",
      "matchManagers": ["github-actions"],
      "matchFileNames": ["templates/**"],
      "pinDigests": false
    },
    {
      "description": "This repo's own workflows are pinned to commit SHAs. Unlike the template files, these are executed rather than copied, so a moved tag is a supply-chain risk rather than a cosmetic one.",
      "matchManagers": ["github-actions"],
      "matchFileNames": [".github/workflows/**"],
      "pinDigests": true
    }
  ]
}
